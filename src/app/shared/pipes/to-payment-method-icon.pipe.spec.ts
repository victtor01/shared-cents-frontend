import { ToPaymentMethodIconPipe } from './to-payment-method-icon.pipe';

describe('ToPaymentMethodIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPaymentMethodIconPipe();
    expect(pipe).toBeTruthy();
  });
});
