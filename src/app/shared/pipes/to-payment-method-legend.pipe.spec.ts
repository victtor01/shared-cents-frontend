import { ToPaymentMethodLegendPipe } from './to-payment-method-legend.pipe';

describe('ToPaymentMethodLegendPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPaymentMethodLegendPipe();
    expect(pipe).toBeTruthy();
  });
});
