import { AppMoneyInputDirective } from './app-money-input.directive';

describe('AppMoneyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMoneyInputDirective();
    expect(directive).toBeTruthy();
  });
});
